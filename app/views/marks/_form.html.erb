<% form_for(@mark) do |f| %>
  <%= f.error_messages %>

  <%= f.label 'name' %>
  <%= f.text_field :name %>
  <p>
    <% if action_name == 'new' %>
    <%= f.submit "Create" %>
    <% else %>
    <%= f.submit "Edit" %>
    <% end %>
  </p>
<% end %>
