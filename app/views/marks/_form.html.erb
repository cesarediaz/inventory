<% form_for(@mark) do |f| %>
  <%= f.error_messages %>

  <%= t('marks.mark') %>
  <%= f.text_field :name %>
  <p>
    <% if action_name == 'new' %>
    <%= f.submit t('marks.create') %>
    <% else %>
    <%= f.submit t('marks.edit') %>
    <% end %>
  </p>
<% end %>
