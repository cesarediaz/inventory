<% form_for(@bill) do |f| %>
  <%= f.error_messages %>

  <% content_tag :div, :id => 'quarter' do %>
  <%= t('bill.company') %>
  <%= collection_select 'bill', 'company_id', Company.find(:all), 'id', 'name',
      {:prompt => true} %>
  <% end %>

  <% content_tag :div, :id => 'quarter' do %>
  <%= t('bill.code') %>
  <%= f.text_field :code %>
  <% end %>

  <% if action_name == 'new' %>
  <%= f.submit t('bill.create') %>
  <% else %>
  <%= f.submit t('bill.edit') %>
  <% end %>

<% end %>
