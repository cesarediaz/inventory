<% if logged_in? %>
  <% if is_an_admin_user? %>
    <%= render :partial => 'list' %>
  <% end %>
<% end %>

<%= will_paginate @users %>

<%= content_tag('br') %>

<%= link_to t('menu.new_account'), :controller => 'users', :action => 'new' %>
